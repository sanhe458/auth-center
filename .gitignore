# 依赖
node_modules/
docs-src/node_modules/

# 文档构建产物（源码在 docs-src/）
docs/
docs-src/.vitepress/dist/

# 敏感配置（复制 config.example.php 为 config.php 使用，密钥绝不上传）
api/config.php
/config.php

# 官方安卓应用种子脚本含环境变量读取，禁止把真实 secret 写进仓库
# （脚本本身可公开，secret 走 AUTHCENTER_ANDROID_SECRET 环境变量）

# Android 构建产物（含内嵌凭据的 BuildConfig）
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/release/
*.apk
*.aab
*.jks
*.keystore

# 备份与临时文件
*.bak
*.log
.DS_Store
*.zip

# AJ-Captcha composer vendor（生产已部署全量，仓库只留源码）
ajcaptcha/vendor/
